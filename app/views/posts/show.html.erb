<style>
  body{
    margin: 0;
  }
</style>
<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@post.image.url, width: "200px") if @post.image && @post.image.url   %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

<%= render 'partials/foot' %>